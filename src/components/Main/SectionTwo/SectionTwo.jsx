import React from 'react';
import css from './SectionTwo.module.css';

const SectionTwo = () => {
    return (
        <section className={`${css.sectionTwo}`}>
            <div className={''}>обо мне</div>
        </section>
    );
};

export default SectionTwo;
